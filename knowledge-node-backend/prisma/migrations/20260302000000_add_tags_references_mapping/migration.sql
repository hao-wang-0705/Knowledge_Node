-- This migration adds Prisma schema mapping for existing tags and references columns.
-- The columns already exist in the database from the initial migration (20260211095133_init).
-- No DDL changes needed - this is a schema-only alignment.
SELECT 1;
